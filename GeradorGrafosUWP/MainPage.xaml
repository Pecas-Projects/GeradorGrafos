<Page
    x:Class="GeradorGrafosUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GeradorGrafosUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local1="using:GeradorGrafosCore"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="#C4C4C4">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Foreground="Black" Grid.ColumnSpan="3" Text="GERADOR DE GRAFOS" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1" Grid.ColumnSpan="3" VerticalAlignment="Center" Margin="50,0,0,0">
            <RadioButton Foreground="Black" Content="Grafo não dirigido" GroupName="TipoGrafo" FontSize="25" IsChecked="True" Checked="naoDirigido_Checked"/>
            <RadioButton Foreground="Black" Content="Grafo dirigido" GroupName="TipoGrafo" FontSize="25" Checked="dirigido_Checked"/>
        </StackPanel>

        <StackPanel Grid.Row="2" VerticalAlignment="Center" Margin="50,0,0,0">
            <TextBlock Foreground="Black" Text="Criar vértice" FontSize="25" />
            <StackPanel Orientation="Horizontal">
                <TextBox Foreground="Black" Header="Informação" x:Name="inputInformacao" FontSize="20" Width="200" Background="White" TextChanged="TextBox_Informacao"/>
                <Button Background="#8E8B8B" VerticalAlignment="Bottom" FontFamily="Segoe MDL2 Assets" FontWeight="Bold"  Content="&#xE710;" Foreground="#000" FontSize="20" Padding="10" CornerRadius="100" Margin="20,0,0,0" Click="Button_addVertice"/>
            </StackPanel>
        </StackPanel>
        <StackPanel Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" VerticalAlignment="Center" Margin="50,0,0,0">
            <TextBlock Foreground="Black" Text="Criar aresta" FontSize="25" />
            <StackPanel Orientation="Horizontal">
                <ComboBox Foreground="Black" Header="Vértice de saída" FontSize="20" Width="200" Background="White" x:Name="ComboBox_Vertices_Saida"/>
                <ComboBox Foreground="Black" Header="Vértice de Entrada" FontSize="20" Width="200" Background="White" x:Name="ComboBox_Vertices_Entrada"/>
                <TextBox Foreground="Black"  Header="Peso" FontSize="20" Margin="20,0,0,0" Width="200" Background="White"  x:Name="InputPeso"/>
                <Button Background="#8E8B8B" VerticalAlignment="Bottom" FontFamily="Segoe MDL2 Assets" FontWeight="Bold"  Content="&#xE710;" Foreground="#000" FontSize="20" Padding="10" CornerRadius="100" Margin="20,0,0,0" Click="Button_AddArco"/>
            </StackPanel>
        </StackPanel>
        <StackPanel Grid.Row="3" Margin="50,20,0,0">
            <TextBlock Text="Vértices adicionados" FontSize="25" />
            <ListView ItemsSource="{x:Bind Grafo.Vertices}" Background="White">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local1:Vertice">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
                            <TextBlock Text="{x:Bind InformacoesVertice()}" FontSize="20"/>
                            <Button Background="#8E8B8B" VerticalAlignment="Top" FontFamily="Segoe MDL2 Assets" FontWeight="Bold"  Content="&#xE711;" Foreground="#000" FontSize="20" Padding="10" CornerRadius="100" Margin="20,0,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        
        <StackPanel Grid.Row="3" Grid.Column="2" VerticalAlignment="Bottom">
            <ComboBox Background="#8E8B8B" x:Name="FontsCombo" Header="Estrutura utilizada" HorizontalAlignment="Center" FontSize="20" Padding="10" Margin="0,0,0,20" Width="230" ItemsSource="{x:Bind Estruturas}"/>
            <Button Background="#8E8B8B" HorizontalAlignment="Center" Content="Visualizar grafo" FontSize="20" Padding="10" Margin="0,0,0,20" Width="200"/>
        </StackPanel>
    </Grid>
</Page>
